{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "name": "subscribeWebSocket",
  "description": "Opens a WebSocket connection and writes parsed messages to the specified data model path. Enables bidirectional real-time communication for live-updating surfaces.",
  "backedBy": "WebSocket",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The WebSocket endpoint URL to connect to (ws:// or wss://)."
      },
      "targetPath": {
        "type": "string",
        "description": "The JSON pointer path in the data model where each parsed message will be written."
      },
      "messageParser": {
        "type": "string",
        "enum": ["json", "text"],
        "default": "json",
        "description": "Parser to apply to incoming WebSocket messages. 'json' (default) parses each message as JSON. 'text' writes the raw string value."
      }
    },
    "required": ["url", "targetPath"],
    "additionalProperties": false
  }
}
