{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "name": "not",
  "description": "Logical NOT of a value. Returns the boolean negation of the resolved value.",
  "parameters": {
    "type": "object",
    "properties": {
      "value": {
        "oneOf": [
          { "type": "string" },
          { "type": "number" },
          { "type": "boolean" },
          {
            "type": "object",
            "properties": {
              "path": {
                "type": "string",
                "description": "A JSON pointer path into the data model to resolve the value dynamically."
              }
            },
            "required": ["path"],
            "additionalProperties": false
          }
        ],
        "description": "The value to negate. Can be a literal or a dynamic reference."
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
