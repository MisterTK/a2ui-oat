{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "name": "numeric",
  "description": "Validates that a value is numeric. Returns true if the value can be parsed as a valid number.",
  "parameters": {
    "type": "object",
    "properties": {
      "value": {
        "oneOf": [
          { "type": "string" },
          { "type": "number" },
          {
            "type": "object",
            "properties": {
              "path": {
                "type": "string",
                "description": "A JSON pointer path into the data model to resolve the value dynamically."
              }
            },
            "required": ["path"],
            "additionalProperties": false
          }
        ],
        "description": "The value to validate. Can be a literal or a dynamic reference to a data model path."
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
