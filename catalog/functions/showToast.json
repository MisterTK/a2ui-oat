{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "name": "showToast",
  "description": "Triggers a toast notification from any action. Displays a temporary message to the user with configurable variant, duration, and position.",
  "backedBy": "Oat Toast WC",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The message text to display in the toast notification."
      },
      "variant": {
        "type": "string",
        "enum": ["success", "warning", "error", "info"],
        "default": "info",
        "description": "The visual variant of the toast indicating severity or intent. Defaults to 'info'."
      },
      "duration": {
        "type": "number",
        "default": 3000,
        "description": "How long the toast is displayed in milliseconds before auto-dismissing."
      },
      "position": {
        "type": "string",
        "enum": ["top-left", "top-center", "top-right", "bottom-left", "bottom-center", "bottom-right"],
        "default": "top-right",
        "description": "Screen position for the toast container. Defaults to 'top-right'."
      }
    },
    "required": ["text"],
    "additionalProperties": false
  }
}
